datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
    - name: Prometheus
      type: Prometheus
      url: http://prometheus-app-server.prometheus
      access: proxy
      isDefault: true
